<UserControl x:Class="TECUserControlLibrary.UserControls.PropertyControls.CostBatchPropertiesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TECUserControlLibrary.UserControls"
             x:Name="parent"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid DataContext="{Binding ElementName=parent, Path=Selected}">
        <ItemsControl ItemsSource="{Binding CostBatch.AllCosts}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock FontSize="14" Text="{Binding Type, StringFormat={}{0} Costs}" 
                               Grid.ColumnSpan="2" Foreground="Black"/>
                        <TextBlock Text="{Binding Cost, StringFormat=Material: {0:C}}" Foreground="Black" Grid.Row="1"/>
                        <TextBlock Text="{Binding Labor, StringFormat=Labor (hrs): {0:F2}}" Foreground="Black" Grid.Row="2"/>
                        <Border Grid.Row="2" BorderThickness="0 0 0 1" Grid.ColumnSpan="2"
                                BorderBrush="Black"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
