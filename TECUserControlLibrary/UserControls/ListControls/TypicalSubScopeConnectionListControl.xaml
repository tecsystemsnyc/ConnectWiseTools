<local:BaseListControl 
    x:TypeArguments="models:TypicalSubScope"
    x:Class="TECUserControlLibrary.UserControls.ListControls.TypicalSubScopeConnectionListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TECUserControlLibrary.UserControls.ListControls"
    xmlns:models="clr-namespace:TECUserControlLibrary.Models"
    xmlns:itemControls="clr-namespace:TECUserControlLibrary.UserControls.ItemControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
    xmlns:dd="urn:gong-wpf-dragdrop"
    x:Name="typicalSubScopeConnectionListControl">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../TECResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid DataContext="{Binding ElementName=typicalSubScopeConnectionListControl}">
        <ListView ItemsSource="{Binding Source}"
                  SelectedItem="{Binding SelectedItem}"
                  dd:DragDrop.IsDropTarget="True"
                  dd:DragDrop.DropHandler="{Binding DropHandler}"
                  AllowDrop="True"
                  Drop="ListView_Dropped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <itemControls:SubScopeConnectionItemControl
                        TypicalSubScope="{Binding}"
                        ConduitTypes="{Binding ElementName=typicalSubScopeConnectionListControl, Path=ConduitTypes}"
                        ReadOnly="{Binding ElementName=typicalSubScopeConnectionListControl, Path=ReadOnly}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:BaseListControl>
